apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
    spec:
      containers:
      - name: fastapi
        image: <AWS_ACCOUNT_ID>.dkr.ecr.ap-south-2.amazonaws.com/st7/fastapi:latest
        ports:
        - containerPort: 8000
        env:
        - name: DB_HOST
          value: "DB_HOST_VALUE"
        - name: DB_USER
          value: "DB_USER_VALUE"
        - name: DB_PASS
          value: "DB_PASS_VALUE"
        - name: DB_NAME
          value: "DB_NAME_VALUE"
---
apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  selector:
    app: fastapi
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: <AWS_ACCOUNT_ID>.dkr.ecr.ap-south-2.amazonaws.com/st7/nginx:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sy-ingress
  annotations:
    kubernetes.io/ingress.class: alb
    # [추가] 그룹화 설정: 다른 인그레스 파일과 이 이름을 똑같이 맞추면 ALB 하나를 공유합니다.
    alb.ingress.kubernetes.io/group.name: "project-group"
    # [추가] AWS ALB 리소스 태그 설정: AWS 콘솔에서 보여질 이름과 정보를 지정합니다.
    alb.ingress.kubernetes.io/tags: "Name=Integrated-ALB,Project=Cloud-Board,Owner=st7"
    
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    alb.ingress.kubernetes.io/certificate-arn: "ACM_ARN_VALUE"
    alb.ingress.kubernetes.io/ssl-redirect: '443'
spec:
  ingressClassName: alb
  rules:
    - host: "www.sy99.cloud"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-service
                port:
                  number: 80
          - path: /board
            pathType: Prefix
            backend:
              service:
                name: nginx-service
                port:
                  number: 80
          - path: /management
            pathType: Prefix
            backend:
              service:
                name: nginx-service
                port:
                  number: 80